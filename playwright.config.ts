/* TODO 1 (see deno.jsonc)
import { defineConfig } from "@playwright/test";

export default defineConfig({
	webServer: {
		command: "deno task preview",
		port: 4173,
	},
	testDir: "src/test/e2e/",
});
*/
